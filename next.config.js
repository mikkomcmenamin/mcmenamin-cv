/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'source.unsplash.com',
      'images.example.com',
      'another-domain.com',
      'img.youtube.com',
    ],
  },
};

module.exports = nextConfig;
