/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['source.unsplash.com', 'images.example.com', 'another-domain.com'],
    },
}

module.exports = nextConfig 